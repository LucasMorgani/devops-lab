apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: prometheus
spec:
  selector:
    app: prometheus
  ports:
  - name: grafana-scraping
    port: 9090 #Porta da API do prometheus
    targetPort: 9090 #Porta da API do prometheus
  type: ClusterIP